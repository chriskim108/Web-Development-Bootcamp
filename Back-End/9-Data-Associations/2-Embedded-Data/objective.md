# Embedding Data (one:one)
* User
* Post
* Procedure
    * Start by setting up Mongoose
    * 2 models
        * Independent
    * Associated the 2 models by adding the post schema array to user schema
    * The user has an array of posts