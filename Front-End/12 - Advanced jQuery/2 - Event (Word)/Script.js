$("input").keypress(function(event){
	//"enter" is the value 13 on the keycode (ascii value)
	//If the user were to hit enter the 
	if(event.which === 13){
		alert("You hit enter");
	}
}); 








